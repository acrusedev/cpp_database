#ifndef TYPES_H
#define TYPES_H

enum class MetaCommandResutls {
	SUCCESS,
	UNRECOGNIZIBLE_COMMAND
};

enum class StatementResult {
	SUCCESS,
	UNRECOGNIZABLE_COMMAND
};

enum class StatementType {
	INSERT,
	UPDATE
};

#endif TYPES_H